export const metadata = {
    title: 'Home | Mail cash',
    description: 'Mailcash',
  }
  
  export default function HomeLayout({ children }) {
  
  
    return (
      <html lang="en">
        <body>
          {children}
        </body>
      </html>
    )
  }